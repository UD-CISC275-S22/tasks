import React from "react";
import { Button } from "react-bootstrap";
import { classes } from "../Interface/classes";

const completedClasses: string[] = [];

function generalChangeStatus(IDX: number): void {
    const words = "hello";
}

function ViewDegree(): JSX.Element {
    return (
        <div>
            <Button>View Degree</Button>
        </div>
    );
}
